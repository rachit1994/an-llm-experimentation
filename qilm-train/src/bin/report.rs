//! report — regenerates results/RESULTS.md from runs/ + gates.toml, checking provenance;
//! refuses to emit fabricated/stale numbers. `report --check` is what CI diffs. STUB (T0.9).
fn main() {
    eprintln!("report: STUB — implement per implementation/tests/PHASE-0.md T0.9");
    std::process::exit(2);
}
